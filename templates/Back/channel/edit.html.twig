{% extends 'Back/base.html.twig' %}

{% block body %}
    <h1>Modifier la chaîne</h1>

    {{ include('Back/channel/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

    <br>

    <a class="btn btn-secondary" href="{{ path('channel_index') }}">Retourner a la liste</a>

    <br><br>

    {{ include('Back/channel/_delete_form.html.twig') }}
{% endblock %}
