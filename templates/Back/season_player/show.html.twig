{% extends 'Back/base.html.twig' %}

{% block title %}{{ season_competition.competition.name }}{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Compétition</th>
                <td><a href="{{ path('season_competition_index', {'season': season_competition.season.id}) }}">{{ season_competition.competition.name }}</a></td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ season_competition.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('season_show', {'id': season_competition.season.id}) }}">back to list</a>

    <a href="{{ path('season_competition_edit', {'season': season_competition.season.id, 'id': season_competition.id}) }}">edit</a>

    {{ include('back/season_competition/_delete_form.html.twig') }}
{% endblock %}
