{% extends 'Back/base.html.twig' %}
{% block title %}Liste des articles{% endblock %}
{% block body %}
    <h1>Confederation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ confederation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ confederation.name }}</td>
            </tr>
            <tr>
                <th>Continent</th>
                <td>{{ confederation.continent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('confederation_index') }}">back to list</a>

    <a href="{{ path('confederation_edit', {'id': confederation.id}) }}">edit</a>

    {{ include('back/confederation/_delete_form.html.twig') }}
{% endblock %}
