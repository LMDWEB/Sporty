{% extends 'Back/base.html.twig' %}

{% block title %}{{ 'season.edit'|trans }}{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ season_competition.id }}</td>
            </tr>
            <tr>
                <th>Compétition</th>
                <td>{{ season_competition.competition.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ season_competition.image }}</td>
            </tr>
            <tr>
                <th>NumberParticipate</th>
                <td>{{ season_competition.numberParticipate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('season_competition_index') }}">back to list</a>

    <a href="{{ path('season_competition_edit', {'id': season_competition.id}) }}">edit</a>

    {{ include('back/season_competition/_delete_form.html.twig') }}
{% endblock %}
