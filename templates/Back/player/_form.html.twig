{{ form_start(form) }}
    {{ form_widget(form) }}


<button class="btn btn-{{ class }}">
    <i class="fas fa-fw fa-{{ icon }}"></i>
    {{ button_name }}
</button>

<input type="hidden" id="widgets-counter" value="0">
    <button type="button" id="add-playerMercatos" class="btn btn-primary">
        <i class="fas fa-plus"></i>
        Ajouter un mercato
    </button>

<a style="margin-top: 20px" class="btn btn-secondary" href="{{ path('player_index') }}">
    {{ 'cancel'|trans }}
</a>

{{ form_end(form) }}