{{ form_start(form) }}
    {{ form_widget(form) }}
<input type="hidden" id="widgets-counter" value="0">
    <button type="button" id="add-playerMercatos" class="btn btn-primary">
        <i class="fas fa-plus"></i>
        Ajouter un mercato
    </button>
    <button class="btn btn-success">{{ 'save'|trans }}</button>
{{ form_end(form) }}