{% extends 'Back/base.html.twig' %}

{% block title %}Partnership{% endblock %}

{% block body %}
    <h1>Partnership</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partnership.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partnership.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ partnership.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ partnership.image }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ partnership.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partnership_index') }}">back to list</a>

    <a href="{{ path('partnership_edit', {'id': partnership.id}) }}">edit</a>

    {{ include('Back/partnership/_delete_form.html.twig') }}
{% endblock %}
